<%#
  Vista para crear un nuevo usuario en el panel de administración.
%>

<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>Nuevo Usuario</h1>
  <%= link_to "Volver", admin_users_path, class: "btn btn-secondary" %>
</div>

<%# Renderizamos el formulario parcial, pasando un nuevo objeto User %>
<%= render 'form', user: @user %>