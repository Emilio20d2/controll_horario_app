<!DOCTYPE html>
<html>
<head>
  <title>Admin - Control Horario</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%# Carga de Bootstrap y los estilos base de la aplicación %>
  <%# El archivo custom.css se carga a través de application.css %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

  <%# Cargamos el mapa de importaciones y el punto de entrada 'main.js' para evitar conflictos %>
  <%= javascript_importmap_tags %>
</head>

<body>
  <div class="container mt-4">
    <div class="content-box">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </div>

  <%# El modal de verificación se renderiza aquí para estar disponible en todas las páginas de admin. %>
  <%= render "layouts/password_verification_modal" %>
</body>
</html>